spring:
  application:
    name: main

  config:
    import:
      - properties/web/main.cors.yml
      - properties/persistence/main.snowflake.yml
      - board.yml
      - comment.yml

server:
  port: 5000

